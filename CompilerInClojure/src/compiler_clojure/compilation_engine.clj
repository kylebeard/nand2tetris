(ns compiler-clojure.compilation-engine
  (:require [compiler-clojure.tokenizer :refer [init-tokenizer advance has-more-tokens token-type token-val curr-token]] [clojure.string :as str] [clojure.java.io :as io]))

(defn init [out])

(defn)